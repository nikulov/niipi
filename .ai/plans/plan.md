# План работы

Общий индекс планов. Конкретные направления — в отдельных файлах.

## Active

- [x] ~~**Перед выкатом — `MAIL_REPLY_TO_ADDRESS` в `.env` прода и stage**~~ —
  дописано 2026-08-12 в `shared/.env` обоих стендов. Заработает после выката:
  в задеплоенном коде `config/mail.php` ещё без `reply_to`, деплой-скрипт сам
  сделает `config:cache`. В коде значения нет — пустая переменная означает
  «без `Reply-To`».
- [ ] **После выката на прод — `media:sync --usages-only`.** Разовый шаг к
  релизу с коммитом `7e33af5`: `Form` начал трекать медиа-usages, но уже
  существующим формам записи в `media_file_usages` никто не проставит. Пока не
  прогнали, вложения старых форм в медиатеке числятся неиспользуемыми, и их
  можно удалить одним кликом — ровно тот сценарий, из-за которого письма
  уходили без файлов. Команда: `sudo -u deploy_niipigrad php artisan media:sync
  --usages-only` из `/var/www/niipigrad-prod/current`. Разбор —
  [archived/mail-attachments.md](archived/mail-attachments.md).
- [ ] **Отбить legacy-фиды на уровне nginx** — незакрытый хвост #27
  ([bug-report-2026-08-04.md](bug-report-2026-08-04.md), раздел «Разобрано —
  не баги приложения»). Сам бот багом не является и разобран ещё 04.08, но
  сделать по итогам разбора ничего не сделали, а он никуда не делся:
  перепроверено 20.08.2026, последний запрос — `20/Aug/2026:11:46:32`, та же
  подсеть `85.192.11.0/24`, те же ~500–700 строк лога в час без ночного
  провала. За 19–20.08 — **10 228 ответов 404** на `/feed/` и
  `/comments/feed/` (плюс столько же 301: каждый опрос логируется дважды,
  см. «Поправку к цифрам» в #27).
  **Зачем:** оба пути проваливаются в catch-all `/{slug}` → `ContentController`
  → запрос в БД ради 404, то есть ~5 000 бесполезных заходов в PHP-FPM в
  сутки. И, что важнее, статистика 404 замусорена настолько, что настоящие
  битые ссылки в ней не разглядеть.
  **Как:** `location = /feed/` и `location = /comments/feed/` с `return 410;`
  в канонический server-блок [infra/nginx/niipigrad-prod.conf](../infra/nginx/niipigrad-prod.conf)
  — до PHP, 410 Gone честно говорит агрегаторам «фида больше нет». Правим
  сервер и референс-копию вместе, порядок раскатки — в
  [infra/nginx/README.md](../infra/nginx/README.md): бэкап в `/root/backup/`,
  `nginx -t`, только потом `systemctl reload nginx`.
  **Не путать с #28:** `/cat/news/*` — это хвост миграции с WordPress,
  ему нужны 301 на новые URL, а не 410.

## Wrapping up

- [Падения публичной формы](public-form-crashes/README.md) — #24
  (`successMessage` + `#[Locked]`) сделан 2026-08-05, **#23** — 2026-08-21.
  Цепочка сначала воспроизведена целиком (неудачная запись файла отдаёт клиенту
  `200 {"paths":[""]}`, дальше пустой путь через снапшот становится мёртвым
  `livewire-tmp/livewire-tmp`), потом закрыта двумя слоями: отсев мёртвых файлов
  в `PublicForm` с ошибкой валидации на поле и `FilesystemException` вокруг
  `validate()` в `SubmitFormAction`. Третьим заходом закрыта видимость: диски
  получили `'report' => true`, слой 1 пишет `Log::warning`, а побочно найденный
  баг #42 (молчаливая потеря вложения заявки — `store()` не возвращает `false`)
  закрыт проверкой `exists()` в `SubmissionFilesStorer`. Тесты —
  `PublicFormUploadCrashTest`, 377 passed. **Хвосты:** `'throw' => true` диску
  `local` отложен решением (цена — гонка в vendor'ном `cleanupOldUploads`), и
  `..` в пути от клиента — отдельная 500 внутри vendor, оставлена осознанно.
- [Репитер соцсетей в блоке «Поделиться + кнопка»](share-button-repeater/README.md) —
  сделано 2026-08-17: набор кнопок шаринга настраивается в админке (SVG +
  шаблон ссылки), пустой репитер оставляет одну основную кнопку. **Хвост:**
  дефолтные иконки `images/icon/{vk,telegram,max}.svg` лежат на public-диске,
  а он не в git — на stage и проде их надо один раз залить (файлы для заливки
  в git: `public/images/icon/`). Пока файлов нет, Filament вычищает дефолт
  репитера при сохранении.

## Отложено

Разобрано, план фикса готов, но браться пока не за что — ждём недостающего
знания. Не архив: сделать надо.

_Пусто._

## Archive

- [Брендовый шаблон письма](archived/email-template.md) — закрыт 2026-08-12:
  вёрстка обёртки и оба письма по заявкам (`fd3be48`, `7ebb25d`), поиск адреса
  клиента по типу поля, блокировка тумблера без поля Email, диагностика
  непосланных писем, `Reply-To` через `MAIL_REPLY_TO_ADDRESS` (`6b38ea1`).
  В админском письме `Reply-To` на заявителя решено **не ставить**.
- [Баг #22 — двойное экранирование в текстовой части](archived/bugs.md) — сделано
  2026-08-11: `renderBodyText()` с `escape: false` и `{!! $textBody !!}` в
  `emails/plain-text.blade.php`. Тесты на оба слоя — рендерер и собранное письмо.
- [Вложения писем теряются молча](archived/mail-attachments.md) — сделано
  2026-08-11: `TracksMediaUsage` на `Form` (без него медиатека предлагала удалить
  вложение как неиспользуемое), джоб сообщает о пропавших файлах в лог и в
  `error_message`, само поле наконец показывается на странице заявки. **Хвост:**
  после выката прогнать `media:sync --usages-only`.
- [Брендовый шаблон для системных писем](archived/system-emails.md) — сделано
  2026-08-11: тема markdown-писем (`resources/views/vendor/mail/`) включает
  `emails.email-template`, css вычищен и заскоплен на `.letter`, строки Laravel
  переведены в `lang/ru.json`, копия `vendor/notifications/email.blade.php` с
  зашитым русским текстом удалена. Сброс пароля, подтверждение email и коды MFA
  проверены в Mailpit.
- [Nginx: HSTS и канонические редиректы](archived/nginx-hsts.md) — раскатано
  2026-07-30: prod получил HSTS + security headers + TLS 1.2/1.3 + HTTP/2 +
  PHP-restriction + `/storage/`-защиту + `$realpath_root`; stage — HSTS
  `max-age=300` + HTTP/2. 2026-08-05 добавлен catch-all `zz-catch-all` — закрыт
  баг #25 (сайт отдавался по голому IP). Followup 2026-08-05: **SSL Labs A+**
  без предупреждений. Закрыт 2026-08-10 вместе с последним хвостом (OPcache /
  `$realpath_root`: `validate_timestamps=On` делает залипание кода невозможным
  независимо от деплоя). Конфиги живут в [infra/nginx/](../infra/nginx/).
- [Баг #41 — почта не уходила с прода](bug-report-2026-08-06.md) — SMTP рвался
  на проверке TLS: `mail.niipigrad.ru` отдавал чужой сертификат reg.ru. Закрыт
  2026-08-10 — хостер выписал SSL на `mail.niipigrad.ru` (правильный вариант
  из плана), `MAIL_HOST` в `.env` менять не пришлось. **Хвост:** два джоба
  заявки 1108 так и висят в `failed_jobs`, письма не ушли — см. #40.
- [Блок «Поделиться + кнопка»](archived/share-button.md) — блок `share-button`
  (копия `button` + шаринг): триггер слева, полоска выезжает вправо поверх
  кнопки, набор соцсетей и иконки зашиты в шаблон — в админке не настраиваются.
  Адреса собирает Alpine из `location.href`, у MAX — диплинк `:share?text=`.
  Сделано 2026-08-06. Хвост: `icon-link` остался заглушкой.
- [Закреплённые проекты в блоке «Проекты»](archived/projects-block-pinned.md) —
  `data.projectIds` в `projects-block`: выбранные идут первыми, остаток
  добирается автоподбором. Заодно починен ключ количества (`quantity` →
  `limit`), который рендерер не читал. Сделано 2026-08-05.
- [Ручной порядок проектов](archived/project-sort-order.md) — поле
  `sort_order` у Project: `1, 2, 3…` сверху, `0`/`null` — в конец, внутри
  одного значения по дате. `scopeOrdered()` + `ProjectsQuery`, поле и
  колонка в Filament. Сделано 2026-08-05.
- [Идемпотентность писем по заявкам](archived/form-mail-idempotency.md) —
  баг #2, разобран и **закрыт 2026-08-04 как won't fix**: код оставлен как
  есть, риск дублей принят, готовый план фикса сохранён в файле.
- [Закрытые баги](archived/bugs.md) — #1–#21 с полными
  обоснованиями и принятыми остаточными рисками. Нумерация сквозная с
  [bugs.md](bugs.md), номера не переиспользуются.
- [Блок «Тематическая подборка»](archived/related-thematic-block.md) —
  полиморфный блок для Post/Project: сетка карточек по категориям текущей
  записи, `excludeId` в `NewsQuery`/`ProjectsQuery`, дефолтная вставка в
  main-секцию перед `CategoryList`. Committed 2026-08-03 (`2feaf76`,
  `aa49322`), кнопка «Смотреть все» убрана по ревью 2026-08-04.
- [Копирование сущности](archived/duplicate-entity.md) — row action
  «Копировать» для Post/Project/Page/Form: суффиксы «(копия N)» /
  `-copy-N`, сброс статуса, клон пивотов и HasMany, `CopyAction` с
  явной авторизацией по `create` ability. Committed 2026-07-30 (`baa1e52`).
- [Динамический sitemap.xml](archived/sitemap.md) — контроллер + view +
  тегированный кэш `sitemap`, флаш на `saved`/`deleted` у Page/Post/Project,
  строка `Sitemap:` в `robots.txt`. Committed 2026-07-29..30
  (`a2bfc9a`, `2840ad7`).
- [Блок «Якорь»](archived/anchor-block.md) — служебный блок контент-билдера:
  пустой `<div id="…">` для хеш-ссылок, уникальность slug через всю форму
  (включая Tabs/Modal), доступен во всех 6 секциях. Committed 2026-07-29 (`16b2c55`).
- [Медиа-менеджер](archived/media-manager.md) — каталог `media_files`, автотрекинг
  usages, Filament-ресурс, пикер во все `FileUpload`. Merged 2026-07-29.

## Планы

- [tests.md](tests.md) — покрытие тестами: инвентаризация, что покрыто, что нет,
  приоритетный список тестов к написанию. Упавшие тесты починены.
- [bugs.md](bugs.md) — **открытого нет**: #42 (молчаливая потеря вложения
  заявки при сбое записи на `public`) заведён и закрыт 2026-08-21. Исходные 22
  пункта закрыты, #22
  (текстовая часть письма) — 2026-08-11. Заведено 22 — 14 из deep-review
  2026-07-14, #15–#16 из ревью published_at, #17–#21 из ревью 2026-08-04, #22 из
  разбора #14; разборы в [archived/bugs.md](archived/bugs.md).
- [bugs-checklist.md](bugs-checklist.md) — компактный чек-лист по [bugs.md](bugs.md),
  [bug-report-2026-08-04.md](bug-report-2026-08-04.md) и
  [bug-report-2026-08-06.md](bug-report-2026-08-06.md).
- [bug-report-2026-08-06.md](bug-report-2026-08-06.md) — инцидент 06.08: #41,
  почта с прода не уходила из-за расхождения CN в TLS-сертификате SMTP-хоста.
  **Закрыт 2026-08-10** хостером (SSL на `mail.niipigrad.ru`). Разбор механизма
  оставлен: если reg.ru снова подменит сертификат, симптом будет тот же.
  Там же правка к #40 (потерянных писем стало десять).
- [bug-report-2026-08-04.md](bug-report-2026-08-04.md) — срез с прода по логам
  на 2026-08-04: #23–#40. P0 — падение публичной формы (#23, #24), по ним
  разобран механизм. #26 (`file_get_contents` по URL в Blade) исправлен
  2026-08-05. #27–#30 разобраны: #27 (legacy-фиды) и #30
  (favicon) сняты как не-баги, #28 переформулирован в редиректы с WP-URL,
  #29 (sitemap) чинится выкаткой. #40 закрыт 2026-08-11 — `failed_jobs` на проде
  вычищен через `queue:flush`, письма решено не оживлять. Остальные пункты —
  только симптомы.

## Соглашения

- Файл на направление. Индекс (этот файл) держим коротким.
- В каждом плане: цель → текущее состояние → бэклог задач (по приоритету) → definition of done.
- Формулировки в императиве («написать …», «починить …»).
- Когда задача сделана — вычёркиваем в файле плана и, если результат затрагивает будущие сессии, отражаем в `.ai/` по правилам из `~/.claude/CLAUDE.md`.

## Порядок работы

1. Открыть план направления → взять верхнюю невыполненную задачу.
2. Мелкие задачи (1 файл, 1–2 теста) — делать сразу.
3. Крупные (интеграции, миграции, рефакторинг) — сначала обсудить подход.
4. После завершения задачи — прогон затронутых тестов через
   `vendor/bin/sail artisan test --compact --filter=<...>`.
